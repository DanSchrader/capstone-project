import styled from 'styled-components';

export default function LoadingPlaceholder() {
  return <PlaceholderContainer></PlaceholderContainer>;
}

const PlaceholderContainer = styled.div`
  display: none;
`;
